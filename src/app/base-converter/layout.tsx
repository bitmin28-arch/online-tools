import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("base-converter");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
