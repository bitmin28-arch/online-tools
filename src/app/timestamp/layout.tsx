import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("timestamp");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
