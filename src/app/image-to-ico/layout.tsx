import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("image-to-ico");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
