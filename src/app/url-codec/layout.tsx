import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("url-codec");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
