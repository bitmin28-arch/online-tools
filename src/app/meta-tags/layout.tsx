import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("meta-tags");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
