import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("box-shadow");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
