import { getToolMetadata } from "../seo-config";
export const metadata = getToolMetadata("jwt-decoder");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
